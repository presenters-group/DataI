import { MovingDirective } from './moving.directive';

describe('MovingDirective', () => {
  it('should create an instance', () => {
    const directive = new MovingDirective();
    expect(directive).toBeTruthy();
  });
});
